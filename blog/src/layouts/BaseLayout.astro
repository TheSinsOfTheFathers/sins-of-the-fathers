---
import '../styles/global.css'; 

interface Props {
	title: string;
  description?: string;
}

const { title, description = "Development journal and author notes for the Ravenwood & Havi saga." } = Astro.props;
const isPostPage = Astro.url.pathname.startsWith('/post/');
const returnUrl = isPostPage ? '/' : 'https://thesinsofthefathers.com';
const pageUrl = Astro.url.href;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title} | The Sins of the Fathers — Author's Log</title>
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={pageUrl} />
    <meta property="og:type" content="website" />
    <meta name="theme-color" content="#050505" />

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700&family=Lato:ital,wght@0,300;0,400;0,700;1,300&family=Courier+Prime:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet" />
    
    <link rel="alternate" type="application/rss+xml" title="TSOF Author's Log" href="/rss.xml" />
    <link rel="icon" type="image/x-icon" href="https://thesinsofthefathers.com/favicon.ico" />
  </head>

  <body class="flex flex-col min-h-screen">
    
    <!-- Atmospheric Background Elements -->
    <div class="fixed inset-0 pointer-events-none -z-10">
      <div class="absolute inset-0 bg-[#050505]"></div>
      <!-- Subtle radial gradient center glow -->
      <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[800px] h-[400px] bg-[radial-gradient(ellipse_at_center,rgba(197,160,89,0.04)_0%,transparent_70%)]"></div>
      <!-- Grid pattern -->
      <div class="absolute inset-0 opacity-[0.015]" style="background-image: linear-gradient(rgba(255,255,255,0.5) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.5) 1px, transparent 1px); background-size: 40px 40px;"></div>
    </div>

    <!-- HEADER -->
    <header class="sticky top-0 z-50 border-b border-white/5 bg-black/70 backdrop-blur-xl">
      <div class="max-w-7xl mx-auto px-6 py-0 flex items-stretch justify-between gap-4">
        
        <!-- Logo -->
        <a href="/" class="flex items-center gap-3 py-4 group">
          <div class="flex flex-col">
            <span class="font-serif text-base text-white tracking-[0.15em] uppercase leading-tight group-hover:text-[#c5a059] transition-colors">
              The Sins of the Fathers
            </span>
            <span class="font-mono text-[9px] text-[#c5a059]/60 tracking-[0.3em] uppercase">
              Author's Log &nbsp;// &nbsp;Development Journal
            </span>
          </div>
        </a>

        <!-- Nav -->
        <nav class="hidden md:flex items-center text-[10px] font-mono uppercase tracking-[0.2em]">
          <a href="/" class="h-full flex items-center px-5 text-gray-500 hover:text-white hover:bg-white/5 transition-all border-r border-white/5 py-4">
            Archives
          </a>
          <!-- No standalone decisions page -->
        </nav>

        <!-- Right side: Return + Auth Status -->
        <div class="flex items-center gap-3">
          <!-- Auth Status (populated by client JS) -->
          <div id="blog-auth-status" class="hidden md:flex items-center gap-3">
            <div class="flex items-center gap-2 text-[10px] font-mono text-gray-600">
              <span class="w-2 h-2 rounded-full bg-gray-700"></span>
              <span id="auth-label">Checking...</span>
            </div>
            <div id="auth-actions" class="hidden flex items-center gap-2">
              <a href="/login" class="text-[9px] font-mono text-[#c5a059] border border-[#c5a059]/30 px-2 py-1 hover:bg-[#c5a059]/10 transition-all uppercase tracking-widest">Sign In</a>
            </div>
            <div id="auth-user-actions" class="hidden flex items-center gap-2">
              <button id="logout-btn" class="text-[9px] font-mono text-gray-500 hover:text-white transition-all uppercase tracking-widest">Logout</button>
            </div>
          </div>

          <a href={returnUrl} class="flex items-center gap-2 text-[10px] font-mono text-gray-400 hover:text-[#c5a059] border border-white/10 hover:border-[#c5a059]/40 px-4 py-2 transition-all">
            ← Main Site
          </a>
        </div>
      </div>
    </header>

    <!-- MAIN CONTENT -->
    <main class="flex-1 w-full">
      <slot />
    </main>

    <!-- FOOTER -->
    <footer class="mt-24 border-t border-white/5 bg-black/50">
      <div class="max-w-7xl mx-auto px-6 py-12 grid grid-cols-1 md:grid-cols-3 gap-8">
        
        <div>
          <p class="font-serif text-white text-sm mb-2 tracking-widest uppercase">TSOF — Author's Log</p>
          <p class="text-xs text-gray-600 font-light leading-relaxed">
            Behind the scenes of the Ravenwood & Havi saga. Writing decisions, character notes, and latest updates.
          </p>
        </div>

        <div>
          <p class="text-[10px] font-mono text-[#c5a059] uppercase tracking-widest mb-3">Navigation</p>
          <ul class="space-y-2 text-xs text-gray-500 font-mono">
            <li><a href="/rss.xml" class="hover:text-[#c5a059] transition-colors">// RSS Feed</a></li>
          </ul>
        </div>

        <div>
          <p class="text-[10px] font-mono text-[#c5a059] uppercase tracking-widest mb-3">Main Site</p>
          <a href="https://thesinsofthefathers.com" class="text-xs text-gray-500 font-mono hover:text-white transition-colors flex items-center gap-2">
            <span class="status-dot"></span>
            thesinsofthefathers.com
          </a>
        </div>
      </div>

      <div class="border-t border-white/5 py-4 text-center">
        <p class="text-[10px] font-mono text-gray-700 uppercase tracking-widest">
          © 2025 The Sins of the Fathers. All records classified.
        </p>
      </div>
    </footer>

    <!-- Auth State Sync -->
    <script>
      import { app, auth } from '../lib/firebase';
      import { 
        onAuthStateChanged, 
        setPersistence, 
        browserLocalPersistence 
      } from "firebase/auth";

      const statusEl = document.getElementById('blog-auth-status');

      // Set persistence and then handle auth state
      (async () => {
        try {
          await setPersistence(auth, browserLocalPersistence);
          console.log(" > Auth: Persistence set to LOCAL.");
        } catch (e) {
          console.error(" > Auth: Persistence error:", e);
        }

        onAuthStateChanged(auth, (user) => {
          console.log(" > Auth: State changed. User:", user ? user.email : "Guest");
          if (!statusEl) return;
          statusEl.classList.remove('hidden');
          statusEl.classList.add('flex');

          const dot = statusEl.querySelector('.rounded-full');
          const label = document.getElementById('auth-label');
          const actions = document.getElementById('auth-actions');
          const userActions = document.getElementById('auth-user-actions');

          if (user) {
            if (dot) { dot.className = 'w-2 h-2 rounded-full bg-green-500 shadow-[0_0_8px_rgba(34,197,94,0.4)]'; }
            if (label) {
              label.textContent = user.displayName || user.email || 'Connected';
              label.className = 'text-green-400/70 font-mono';
            }
            if (actions) actions.classList.add('hidden');
            if (userActions) userActions.classList.remove('hidden');
            localStorage.setItem('blog_uid', user.uid);
            localStorage.setItem('blog_auth_checked', 'true');
          } else {
            // Check for cross-subdomain sync cookie
            const hasActiveSession = document.cookie.includes('tsof_session_active=true');
            
            if (dot) { dot.className = 'w-2 h-2 rounded-full bg-gray-600'; }
            if (label) { 
              if (hasActiveSession) {
                label.textContent = 'Sync Required';
                label.className = 'text-gold/50 font-mono animate-pulse';
              } else {
                label.textContent = 'Guest'; 
                label.className = 'text-gray-600 font-mono';
              }
            }
            if (actions) actions.classList.remove('hidden');
            if (userActions) userActions.classList.add('hidden');
            
            // If they are logged in on main site, suggest login here
            if (hasActiveSession && actions) {
              const loginBtn = actions.querySelector('a');
              if (loginBtn) {
                loginBtn.textContent = 'Sync Vault';
                loginBtn.classList.add('border-gold', 'text-gold');
              }
            }

            localStorage.removeItem('blog_uid');
            localStorage.setItem('blog_auth_checked', 'true');
          }
        });

        // Logout listener
        document.getElementById('logout-btn')?.addEventListener('click', async () => {
          console.log(" > Auth: Logout requested.");
          const { signOut } = await import('firebase/auth');
          await signOut(auth);
          localStorage.removeItem('blog_uid');
          console.log(" > Auth: Signed out, reloading...");
          window.location.reload();
        });
      })();
    </script>

  </body>
</html>