---
import { createClient } from "@sanity/client";
import { urlFor } from "../lib/urlFor";
import BaseLayout from "../layouts/BaseLayout.astro";

interface Post {
  title: string;
  slug: { current: string };
  publishedAt: string;
  excerpt: string;
  mainImage: any;
  authorName: string;
}

const client = createClient({
  projectId: "8cfeoaz2", 
  dataset: "production",         
  useCdn: true,                  
  apiVersion: "2023-05-03",      
});

const posts: Post[] = await client.fetch(`*[_type == "post"] | order(publishedAt desc) {
  title,
  slug,
  publishedAt,
  excerpt,
  mainImage,
  authorName, 
}`); 

const featuredPost = posts[0];
const remainingPosts = posts.slice(1);

function formatDate(dateString: string | null) {
  if (!dateString) return 'Undated';
  return new Date(dateString).toLocaleDateString('tr-TR', {
    year: "numeric",
    month: "long",
    day: "numeric",
  });
}
---

<BaseLayout title="Author's Log">
  
  <!-- HERO SECTION -->
  <section class="relative border-b border-white/5 overflow-hidden">
    <div class="max-w-7xl mx-auto px-6 py-20 md:py-28 relative z-10">
      <div class="animate-fade-up">
        <div class="flex items-center gap-3 mb-6">
          <span class="status-dot"></span>
          <span class="hud-badge">YAZIM DEVAM EDİYOR</span>
        </div>
        <h1 class="font-serif text-5xl md:text-7xl text-white leading-tight tracking-tight mb-4">
          Author's <span class="text-[#c5a059]">Log</span>
        </h1>
        <p class="text-gray-400 text-lg md:text-xl max-w-2xl font-light leading-relaxed mb-8">
          Ravenwood & Havi Saga'nın perde arkası. Yazım kararları, karakter notları ve okuyucularla paylaşılan gelişmeler.
        </p>
        <div class="flex flex-wrap gap-3">
          <a href="/decisions" class="inline-flex items-center gap-2 text-xs font-mono uppercase tracking-widest text-[#c5a059] border border-[#c5a059]/40 px-5 py-2.5 hover:bg-[#c5a059] hover:text-black transition-all">
            Kararlar & Anketler →
          </a>
          <a href="/upcoming-books" class="inline-flex items-center gap-2 text-xs font-mono uppercase tracking-widest text-gray-400 border border-white/10 px-5 py-2.5 hover:border-white/40 hover:text-white transition-all">
            Gelecek Kitaplar →
          </a>
        </div>
      </div>
    </div>

    <!-- Decorative corner lines -->
    <div class="absolute top-8 right-8 w-16 h-16 border-t border-r border-[#c5a059]/10 pointer-events-none"></div>
    <div class="absolute bottom-8 left-8 w-16 h-16 border-b border-l border-[#c5a059]/10 pointer-events-none"></div>
  </section>

  <div class="max-w-7xl mx-auto px-6 py-16">

    {posts.length === 0 ? (
      <div class="py-32 text-center border border-dashed border-white/10">
        <p class="font-mono text-[#c5a059] text-xl mb-2">DATABASE EMPTY</p>
        <p class="text-gray-600 text-sm font-mono">İlk yazı henüz eklenmedi.</p>
      </div>
    ) : (
      <>
        <!-- FEATURED POST -->
        {featuredPost && (
          <section class="mb-20 animate-fade-up-delay-1">
            <div class="flex items-center gap-3 mb-6">
              <span class="gold-line"></span>
              <span class="text-[10px] font-mono text-[#c5a059] uppercase tracking-[0.3em]">Son Kayıt</span>
            </div>
            
            <a href={`/post/${featuredPost.slug.current}`} class="featured-card group block md:flex">
              {featuredPost.mainImage ? (
                <div class="relative md:w-[55%] h-72 md:h-auto overflow-hidden">
                  <img
                    src={urlFor(featuredPost.mainImage).width(900).height(600).url()}
                    alt={featuredPost.title}
                    class="w-full h-full object-cover"
                  />
                  <div class="absolute inset-0 bg-gradient-to-r from-transparent to-black/70 hidden md:block"></div>
                  <div class="absolute inset-0 bg-gradient-to-t from-black to-transparent md:hidden"></div>
                </div>
              ) : (
                <div class="md:w-[55%] h-72 md:h-[420px] bg-[#0a0a0a] flex items-center justify-center border-r border-white/5">
                  <span class="font-serif text-6xl text-gray-800 opacity-30">TSOF</span>
                </div>
              )}
              
              <div class="md:w-[45%] p-8 md:p-12 flex flex-col justify-center bg-[#070707]">
                <div class="text-[10px] font-mono text-[#c5a059] uppercase tracking-widest mb-4">
                  {formatDate(featuredPost.publishedAt)}
                  {featuredPost.authorName && <> &nbsp;//&nbsp; {featuredPost.authorName}</>}
                </div>
                <h2 class="font-serif text-3xl md:text-4xl text-white mb-4 leading-tight group-hover:text-[#c5a059] transition-colors">
                  {featuredPost.title}
                </h2>
                {featuredPost.excerpt && (
                  <p class="text-gray-400 text-sm leading-relaxed mb-8 line-clamp-3 font-light">
                    {featuredPost.excerpt}
                  </p>
                )}
                <span class="text-xs font-mono uppercase tracking-widest text-[#c5a059] flex items-center gap-2 group-hover:gap-4 transition-all">
                  Dosyayı Aç <span>→</span>
                </span>
              </div>
            </a>
          </section>
        )}

        <!-- REMAINING POSTS GRID -->
        {remainingPosts.length > 0 && (
          <section class="animate-fade-up-delay-2">
            <div class="flex items-center gap-3 mb-8">
              <span class="gold-line"></span>
              <span class="text-[10px] font-mono text-[#c5a059] uppercase tracking-[0.3em]">Arşiv</span>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
              {remainingPosts.map((post: Post) => (
                <article class="post-card group flex flex-col">
                  {post.mainImage ? (
                    <div class="relative h-48 overflow-hidden border-b border-white/5">
                      <img
                        src={urlFor(post.mainImage).width(600).height(400).url()}
                        alt={post.title}
                        class="w-full h-full object-cover grayscale brightness-75 group-hover:grayscale-0 group-hover:brightness-100 group-hover:scale-105 transition-all duration-700"
                      />
                      <div class="absolute inset-0 bg-gradient-to-t from-black/80 to-transparent"></div>
                    </div>
                  ) : (
                    <div class="h-48 bg-[#0a0a0a] flex items-center justify-center border-b border-white/5">
                      <span class="font-serif text-4xl text-gray-800 opacity-20">TSOF</span>
                    </div>
                  )}
                  
                  <div class="p-6 flex-1 flex flex-col">
                    <div class="text-[9px] font-mono text-[#c5a059]/70 uppercase tracking-widest mb-3">
                      {formatDate(post.publishedAt)}
                    </div>
                    <h2 class="font-serif text-xl text-white mb-3 leading-snug group-hover:text-[#c5a059] transition-colors flex-1">
                      <a href={`/post/${post.slug.current}`} class="hover:underline decoration-[#c5a059]/40 underline-offset-4">
                        {post.title}
                      </a>
                    </h2>
                    {post.excerpt && (
                      <p class="text-xs text-gray-500 line-clamp-2 mb-5 font-light leading-relaxed">
                        {post.excerpt}
                      </p>
                    )}
                    <a href={`/post/${post.slug.current}`} class="text-[10px] font-mono uppercase tracking-widest text-gray-600 hover:text-[#c5a059] transition-colors flex items-center gap-2 group-hover:gap-3">
                      Oku <span>→</span>
                    </a>
                  </div>
                </article>
              ))}
            </div>
          </section>
        )}
      </>
    )}
  </div>

</BaseLayout>